# Python environment for AMMPM: Agentic Multi-Machine Predictive Maintenance
# using Time-Series Foundation Models for Explainable and Trustworthy RUL Prediction.
#
# Pin exact versions for numerical/scientific packages to keep RUL prediction
# experiments reproducible across machines and over time. `torch` is left as a
# floor constraint since the correct build (CPU/CUDA/ROCm) is hardware-specific
# and should be installed per the official instructions for the target rig.

# --- Core deep learning ---
torch>=2.1.0

# --- Numerical / data handling ---
numpy==1.26.4
pandas==2.2.0
scipy==1.12.0
pyarrow==15.0.0

# --- Classical ML / metrics ---
scikit-learn==1.4.0

# --- Explainability (for trustworthy RUL prediction) ---
shap==0.44.0

# --- Agentic orchestration ---
langgraph==0.2.55

# --- Time-series foundation models (TSFM) ---
# Pretrained forecasting backbone used zero-shot (E4+): no task-specific
# fine-tuning, evaluated purely as a frozen feature extractor for RUL.
chronos-forecasting==2.3.1

# --- Visualization ---
matplotlib==3.8.0
seaborn==0.13.0

# --- Interactive development ---
jupyter==1.0.0
ipykernel==6.29.0

# --- Utilities ---
tqdm==4.66.0
